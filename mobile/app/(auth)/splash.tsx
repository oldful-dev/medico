// Splash Screen - App branding & auto-redirect
// Displays Oldful logo animation before navigating to login or home
import { View, Text } from 'react-native';

export default function SplashScreen() {
    return (
        <View>
            <Text>Splash Screen</Text>
        </View>
    );
}

// Profile Setup Screen - Name, Gender, DOB, Address, Emergency Contact
// PRD: Mandatory fields - Gender, Emergency Contact; auto-generates Unique User ID
import { View, Text } from 'react-native';

export default function ProfileSetupScreen() {
    return (
        <View>
            <Text>Profile Setup</Text>
        </View>
    );
}

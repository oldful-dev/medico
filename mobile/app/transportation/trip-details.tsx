// Trip Details - Summary and confirmation
import { View, Text } from 'react-native';
import { useTranslation } from 'react-i18next';

export default function TripDetailsScreen() {
    const { t } = useTranslation();
    return (
        <View>
            <Text>Trip Details</Text>
        </View>
    );
}

// Account Tab - User Profile & Settings
// PRD: Profile info, emergency contacts, address management, language preferences
import { View, Text } from 'react-native';

export default function AccountScreen() {
    return (
        <View>
            <Text>Account Screen</Text>
        </View>
    );
}

// Nurse Care Scheduling
import { View, Text } from 'react-native';
import { useTranslation } from 'react-i18next';

export default function NurseCareScheduleScreen() {
    const { t } = useTranslation();
    return (
        <View>
            <Text>Schedule</Text>
        </View>
    );
}
